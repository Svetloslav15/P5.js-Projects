function setup() {
	createCanvas(windowWidth, windowHeight);
}

function draw() {
	background(150);
	fill(255);
	noStroke();
	translate(width / 2, height / 2);
	rect(25, 25, 50, 50);
	let a = new PVector();
}